name: recollect_utils_example
description: "Demonstrates how to use the recollect_utils plugin."
publish_to: 'none'
environment:
  sdk: ^3.10.8

dependencies:
  flutter:
    sdk: flutter

  recollect_utils:
    path: ../
dev_dependencies:
  integration_test:
    sdk: flutter
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0
flutter:
  uses-material-design: true































